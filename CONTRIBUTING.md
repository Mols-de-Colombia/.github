# Contributing to Mols de Colombia

Welcome to the engineering team! To maintain our quality standards (Software Factory), please follow these guidelines.

## 🌿 GitFlow
1.  **Never push to `main` directly.**
2.  Create a branch from `develop`: `git checkout -b feature/my-cool-feature`.
3.  Use Semantic Commits:
    *   `feat:` New feature
    *   `fix:` Bug fix
    *   `docs:` Documentation
    *   `style:` Formatting (missing semi colons, etc)
    *   `refactor:` Refactoring code
    *   `chore:` Build tools, config changes

## 🏗️ Code Quality
*   We use **TypeScript** in strict mode. No `any`.
*   Ensure **Lighthouse** score remains 95+.
*   Comments should be in **English**.

## 🤝 Pull Requests
*   Fill out the PR template completely.
*   Request a review from at least one Tech Lead.
*   Ensure CI pipeline passes before merging.